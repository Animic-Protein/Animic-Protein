const seeds=[
{name:'Extra nos',type:'principi',label:'Principi',definition:'Allò que ens transforma no necessita haver nascut de nosaltres.',question:'Què pot entrar des de fora que nosaltres sols no hauríem sabut produir?',connections:['Inter nos','Mutatio','Rosa de l’Escolta']},
{name:'Inter nos',type:'principi',label:'Principi',definition:'Allò que apareix en la relació i que cap de les parts posseïa completament abans.',question:'Què està apareixent entre nosaltres que no pertany del tot a cap de nosaltres?',connections:['Extra nos','Mutatio','Zajj Viu']},
{name:'Mutatio',type:'principi',label:'Principi',definition:'Transformació que deixa rastre i modifica les possibilitats del que vindrà després.',question:'Què ha de canviar perquè puguem dir que la trobada realment ha tingut lloc?',connections:['Extra nos','Inter nos','Continuum musicalis']},
{name:'Intuitio Machinae',type:'principi',label:'Principi',definition:'Connexió o possibilitat produïda per una màquina que mereix contrast, no obediència.',question:'Quan una màquina ens sorprèn, estem descobrint una relació fèrtil o projectant sentit sobre una coincidència?',connections:['Extra nos','Inter nos','Mutatio']},
{name:'Felix Error',type:'prova',label:'En prova',definition:'L’error fèrtil: una desviació que revela una possibilitat que el sistema no sabia buscar.',question:'Quan deixem de corregir una desviació i comencem a escoltar què pot arribar a ser?',connections:['Mutatio','Zajj Viu','Compost']}
];
const grid=document.querySelector('#seed-grid');const tpl=document.querySelector('#seed-card-template');
function render(filter='all'){grid.innerHTML='';seeds.filter(s=>filter==='all'||s.type===filter).forEach(seed=>{const node=tpl.content.cloneNode(true);const card=node.querySelector('.card');card.dataset.type=seed.type;node.querySelector('.type').textContent=seed.label;node.querySelector('.name').textContent=seed.name;node.querySelector('.definition').textContent=seed.definition;node.querySelector('.question').textContent=seed.question;const c=node.querySelector('.connections');seed.connections.forEach(x=>{const span=document.createElement('span');span.textContent=x;c.appendChild(span)});grid.appendChild(node)})}
document.querySelectorAll('.filter').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));btn.classList.add('active');render(btn.dataset.filter)}));render();